# Core experiment server (Heroku / local)
PsiTurk==3.3.1
setuptools<81

# Database (Postgres on Heroku; SQLite used locally by default)
SQLAlchemy==1.3.24
psycopg2-binary==2.9.9

# Required by PsiTurk / Heroku stack
cryptography==38.0.4
gunicorn==20.0.4
